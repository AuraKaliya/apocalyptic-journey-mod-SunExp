interface:
  display_name: "SunExp Skill Evolution"
  short_description: "Distill SunExp traces and remove stale skill anchors."
  default_prompt: "Use $sunexp-skill-evolution to analyze SunExp development traces, update project skills, tune triggers, split references, remove stale old-repository assumptions, and add durable validation."
