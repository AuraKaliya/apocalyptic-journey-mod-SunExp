interface:
  display_name: "SunExp Mod Dev"
  short_description: "Build and debug SunExp runtime and content."
  default_prompt: "Use $sunexp-mod-dev to implement or debug SunExp C# runtime, Managed compatibility, multiplayer hooks, CSV content, and validation."
