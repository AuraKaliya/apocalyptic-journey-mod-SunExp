# Role, Dialogue, Event Checklist

- Add Data and Text rows together.
- Keep IDs stable and unique.
- Verify every role/image/animation path.
- Keep `ChoiceCount` aligned with scripts and localized choice text.
- Use `CS.ScriptExecutor.PlayerInfo` APIs with nil-safe state handling.
- Test in game after automated validation.
