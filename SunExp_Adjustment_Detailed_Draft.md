# SunExp 卡包调整细稿

> 目标：先做一轮减法重构，不新增新牌。把当前 30 张卡、13 件遗物、8 个 Buff 的表达压清楚，让玩家从“我在叠层”转成“我在铺火、开冕、引爆”。
> 当前数值与机制判断以 `SunExp/Data/**/sunexp.csv`、`SunExp/Text/**/sunexp.csv`、`SunExp/Scripts/Entry.lua` 和 `_src` 源文件为准。

## 总体目标

本轮调整建议围绕一条明确链路：

`日耀充能 -> 点燃/扩散 -> 聚炎蓄爆 -> 圣冕窗口 -> 炽冕崩落`

每张牌只承担一个主职能。复杂规则放进 Buff 或遗物说明，卡面只写玩家当前最需要判断的内容。

## 调整目标细稿

### 调整方向 01：压缩卡牌池规模

*调整前*

当前 3 个卡包共有 30 张卡。三个分支都在反复触碰 `日耀`、`聚炎`、`炽灼天幕`、`灼烧`、`抽牌`、`立即生效`，导致玩家看到的不是三条路线，而是一堆条件堆叠。

*调整后*

第一轮目标压到 22 张左右。

【日耀：星火】保留 8 张，承担启动、基础生存、圣冕入口。

【日耀：烬冠】保留 8 张，承担聚炎转换、自燃风险、爆发兑现。

【日耀：天幕】保留 6 张，承担敌方灼烧、扩散、群体引爆。

被移出的牌先不删除 ID，只从掉落/卡包池里移出，避免存档、文本、图标引用突然断裂。

*理由*

SunExp 当前的问题不是内容少，而是玩家每次选牌都要读一段机制论文。先压掉重复牌，才能让每个分支有清晰手感。减少掉落池后，玩家更容易拿到关键链路牌，构筑启动也会更快。

### 调整方向 02：压缩卡面文案

*调整前*

多张牌在描述中同时出现获得、吸收、消耗、抽牌、回费、立即生效、条件分支。典型例子是 `炽冕崩落`、`日耀：引燃`、`太阳圣祷`、`日相校准`、`灼流回收`。

*调整后*

普通牌描述控制在 45 字以内。

进阶牌描述控制在 70 字以内。

终结牌描述最多 120 字，但必须分成短句。

描述只写核心行为，不在卡牌里反复解释 Buff 全机制。例如卡牌写“获得 2 层日耀”，具体日耀如何给超凡，放在日耀 Buff 说明里。

*理由*

卡牌游戏中，卡面描述是战斗决策工具，不是规则书。玩家在战斗中只需要知道“这张牌现在做什么”，不需要每张牌都解释完整体系。

### 调整方向 03：重构【日耀：星火】为启动包

*调整前*

【日耀：星火】有 11 张卡。`启辉誓言`、`日耀：引燃`、`太阳圣祷`、`日相校准` 都在做“日耀入口 + 条件收益”。`日耀源核`和`日耀：授冕`同为能力牌，基础包偏慢。

*调整后*

保留并整理为 8 张。

`星火`保留：0 费攻击，造成基础伤害，给目标 1 灼烧，获得 1 日耀。

`耀焰斩`保留：1 费攻击，伤害随日耀提高，圣冕期间获得额外收益。

`烬衣`保留：0 费安全阀，清除自身灼烧并给少量护盾。

`晨光壁垒`保留但简化：只做“按日耀获得护盾”，不再读取聚炎，避免基础包提前引入第二资源。

`日耀：引燃`吸收`启辉誓言`的入口职责：获得日耀，全敌获得灼烧；若没有天幕，获得 1 层天幕。不再抽牌、不再给聚炎、不再立即触发灼烧。

`日耀：回转`保留：移除自身 1 层灼烧并获得日耀；没有灼烧时抽 1 张。

`太阳圣祷`吸收`日相校准`的充能职责：获得较多日耀，并可少量清理自身灼烧。抽牌只保留一个简单阈值。

`日耀：授冕`保留：圣冕标准入口，获得圣冕和日耀。

`启辉誓言`从掉落中移出，职能并入`日耀：引燃`。

`日相校准`从掉落中移出，职能并入`太阳圣祷`。

`日耀源核`从卡包中移出，后续更适合作为遗物或祝福。

*理由*

星火包应该让玩家学会“日耀是什么、圣冕怎么开”，而不是一开始就同时管理日耀、天幕、聚炎、自燃、抽牌。基础包越清楚，后续两个扩展包越有空间。

### 调整方向 04：重构【日耀：烬冠】为爆发包

*调整前*

【日耀：烬冠】有 10 张卡。`日耀灼光`和`燃星之咒`都在做灼烧/聚炎伤害；`凝烬成塔`、`聚炎护盾`、`灼流回收`都在处理聚炎转换和补偿；`聚炎轮转`是被动层数转换，会加重“自动叠层”的感受。

*调整后*

保留并整理为 8 张。

`引炎`保留：吸收目标至多 6 层灼烧，转化为聚炎。它是烬冠包的标准转换牌。

`凝烬成塔`保留但降复杂度：将自身至多 5 层灼烧转化为聚炎，满额时抽 1 张。原本抽 2 张偏强且鼓励堆自燃。

`聚炎护盾`保留：按聚炎层数获得护盾，并消耗少量聚炎。它是烬冠包的安全牌。

`燃星之咒`保留并简化：消耗至多 5 层聚炎造成伤害，并给予目标少量灼烧。删除日耀分支和圣冕额外分支。

`燃冠誓言`保留：消耗至多 12 层聚炎，使敌方全体获得灼烧；消耗达到阈值时抽牌/回费。

`炎轮再临`保留但设置上限：本场第 1/2/3 次使用分别触发全敌灼烧 1/2/3 次，费用也到 3 封顶。第 4 次以后不继续增长。

`灼流回收`保留并简化：触发目标灼烧 1 次，然后移除目标全部灼烧并获得等量聚炎。删除抽牌和护盾补偿。

`炽冕崩落`保留并强化招牌感：全敌大伤，消耗全部聚炎和一半日耀；有圣冕时追加全敌灼烧触发，无圣冕时自身承受同等反噬。

`日耀灼光`从掉落中移出，伤害职责并入`燃星之咒`。

`聚炎轮转`从掉落中移出，避免被动自动叠层掩盖玩家主动操作。

*理由*

烬冠包的爽感应来自“我主动把火收回来，再一口气烧出去”。被动转换越多，玩家越像在看系统自动滚层数；主动转换、主动爆发、主动承担反噬，才是这个包的戏。

### 调整方向 05：重构【日耀：天幕】为扩散包

*调整前*

【日耀：天幕】有 9 张卡。`灼热天幕`和`天幕再临`同样在获得天幕和全体灼烧；`残光病兆`是被动能力牌，容易继续堆复杂触发；`焚污除秽`与主链较弱，像通用负面转换工具。

*调整后*

保留并整理为 6 张。

`灼热天幕`保留：获得 1 层天幕，全体获得灼烧，是天幕包的标准入口。

`烟蚀`保留：对目标造成基础伤害，目标灼烧越高伤害越高。

`日蚀`保留：敌方全体获得灼烧和少量无力，作为天幕包的控制牌。

`燃灾`保留：选择一个敌人，将其灼烧扩散给其他敌人，并触发目标灼烧。

`蚀天之咒`保留：使目标灼烧翻倍，最高不超过 49 层，然后触发一次。

`冠冕威光`保留并强化：全敌获得灼烧；若有天幕，立即触发全敌灼烧；圣冕期间再追加群体伤害。

`天幕再临`从掉落中移出，职能并入`灼热天幕`和`冠冕威光`。

`残光病兆`从掉落中移出。若保留为遗物或祝福，需要降低易伤转化比例。

`焚污除秽`从天幕包移出。它可以作为后续通用防御/净化牌重新设计，不放在天幕主链里。

*理由*

天幕包应该是“把单点火势扩成全场灾厄”，不是另一个日耀/聚炎混合包。保留单点翻倍、群体扩散、群体引爆三个动作，玩家会更容易理解并感到爽。

### 调整方向 06：调整遗物池

*调整前*

当前 13 件遗物中，多个遗物都在做“日耀启动”“自燃转聚炎”“敌方灼烧扩散”。遗物数量偏多，且与卡牌重复。

*调整后*

目标压到 10 件左右。

保留`晨辉碎片`：基础日耀启动。

保留`日心棱镜`：每回合第一次获得日耀后给超凡，服务星火包。

保留`环日镜`：行动次数转化为日耀/敌方灼烧，作为轻量循环遗物。

保留`日相刻盘`：日耀达到 4/8/12 时给一次性收益，强化阶段感。

保留`授冕圣座`：第一次获得圣冕时抽牌和护盾，强化开窗反馈。

保留`太阳瓶`：每回合随机触发敌方灼烧，并把残火转聚炎，服务天幕和烬冠交叉。

保留`小型日轮`：天幕存在时给护盾，并小幅处理自燃。

保留`炽冠圣心`：高稀有核心遗物，直接开启日耀、天幕和圣冕。

保留`曜阳日晷`：全敌都有灼烧时给无力，服务天幕控制路线。

保留`灰烬护符`，并吸收`烬衣衬布`和`聚炎护符`的一部分职责：自身灼烧较高时移除少量灼烧，获得护盾和聚炎。

`烬衣衬布`从掉落中移出。

`聚炎护符`从掉落中移出。

`燃灾风带`从掉落中移出，扩散职责交给卡牌`燃灾`。

*理由*

遗物应放大路线，而不是替玩家自动完成路线。过多自动触发遗物会让玩家进一步觉得自己只是在看层数变化。

### 调整方向 07：优化启动与脚本注册

*调整前*

`Entry.lua` 是运行入口，约 72KB。`registry.lua` 中注册了约 150 个动态方法，`setup.lua` 默认挂了 9 个地图相关 Hook。卡包战斗逻辑、乌娜职业逻辑、地图事件修复逻辑都在启动面上。

*调整后*

保留 `Scripts/Entry.lua` 作为唯一运行入口，不改成运行时 `require`。

`_src` 继续作为构建源，最终仍拼接到 `Entry.lua`。

动态注册分组：战斗核心、乌娜职业、地图事件。

默认注册战斗核心。乌娜职业与地图事件保留函数，但通过配置或明确开关控制 Hook 注册。

地图事件 Hook 单独开关化，普通卡包游玩不需要默认挂满地图修复链。

*理由*

这能减少启动时无关逻辑的负担，也方便定位启动慢。更重要的是保留现有运行模型，避免因为模块化重构引入引擎加载风险。

### 调整方向 08：把爽点从“层数变化”挪到“按钮反馈”

*调整前*

很多强度来自 Buff 的 `StartRound` 或 `Action` 事件，例如日耀给超凡、聚炎导致自燃、天幕每回合给灼烧。玩家操作当下经常只是看到状态层数变化，不一定看到立即反馈。

*调整后*

保留少量被动收益，但每个卡包必须有明确的主动按钮。

星火包的按钮是`日耀：授冕`：开圣冕。

烬冠包的按钮是`炽冕崩落`：把聚炎和日耀兑现为全体爆发。

天幕包的按钮是`冠冕威光`和`蚀天之咒`：全体引爆或单体翻倍引爆。

这些按钮应优先配置更明确的 Action、特效或音效，哪怕数值不变，也要让玩家感觉“我按下去，场面发生了事”。

*理由*

爽感不是只来自最终伤害数字，也来自清楚的因果反馈。玩家要能记住“那张牌一打出去，全场烧起来了”。

## 调整后的卡包结构草案

### 【日耀：星火】草案

定位：启动、充能、开圣冕。

保留或重做为以下 8 张：

- `星火`
- `耀焰斩`
- `烬衣`
- `晨光壁垒`
- `日耀：引燃`
- `日耀：回转`
- `太阳圣祷`
- `日耀：授冕`

移出掉落：

- `启辉誓言`
- `日相校准`
- `日耀源核`

### 【日耀：烬冠】草案

定位：吸火、聚炎、爆发。

保留或重做为以下 8 张：

- `引炎`
- `凝烬成塔`
- `聚炎护盾`
- `燃星之咒`
- `燃冠誓言`
- `炎轮再临`
- `灼流回收`
- `炽冕崩落`

移出掉落：

- `日耀灼光`
- `聚炎轮转`

### 【日耀：天幕】草案

定位：铺灼烧、扩散、群体引爆。

保留或重做为以下 6 张：

- `灼热天幕`
- `烟蚀`
- `日蚀`
- `燃灾`
- `蚀天之咒`
- `冠冕威光`

移出掉落：

- `天幕再临`
- `残光病兆`
- `焚污除秽`

## 标准计算口径

### 灼烧

官方模板 `apocalyptic-journey-mod-tutorial/ModTemplate/Scripts/Lib/DataConfigs/Data/Buff/buff.csv` 中，`buff_burn` 的结算核心为：

```text
ChangeHp(((int)((-1-Self.CurHp/100f)*Self.GetBuff(DataId.buff_burn).buffConfig.Level)).ToString())
```

因此估算时按当前血量计算：

```text
单次灼烧伤害 = 灼烧层数 * (1 + 当前血量 / 100)
```

官方数据中灼烧上限为 49 层，每回合减少 1 层。SunExp 的即时触发应使用：

```lua
self:SetStatus("AllTarget")
self:RunImmediately("buff_burn", "StartRound")
```

多次触发时，每次都会按剩余当前血量重新计算，所以总伤害不是简单乘法。

### 直接伤害

SunExp 当前 helper `SunExp_DealDamage` 会先向下取整，再调用 `self:Damage(...)`。若乌娜余烬加成存在，还会进入 `SunExp_ApplyWunaEmberDamageBonus`。

估算普通卡牌直接伤害时，先按卡牌公式算基础值，再考虑超凡、易伤、锋锐等游戏原生动态变量。

### 超凡

官方文本说明：`buff_extraordinary` 每层提供 1% 基础增伤。SunExp 的日耀 Buff 当前在行动时给 `日耀层数 * 5` 层超凡。

例如 8 层日耀行动一次，会获得 40 层超凡，后续基础伤害约增加 40%。这部分是重要隐藏强度，文案不应每张牌重复写，但数值测试必须纳入。

### 易伤和无力

官方文本说明：`buff_vulnerability` 每层使受伤增加 10%，`buff_weak` 每层使造成伤害降低 1。

这也是建议移出`残光病兆`的原因：如果按敌方灼烧层数一半转易伤，灼烧已经高层时会同时放大直接伤害和后续爆发，容易把数值推到失控。

### 护盾与低血高护盾

护盾通过 `ChangeDefence(...)` 获得，直接伤害通常会先面对护盾。

灼烧使用 `ChangeHp(...)` 路径做生命变化。按模板脚本估算，它更像直接改变当前血量，不是普通护盾伤害路径。实际是否完全绕过所有特殊防护，需要进游戏实测确认。

## 调整后实际玩法与启动流程

### 【日耀：星火】启动流程

目标手感：快速有事做，快速看见日耀成长，第二或第三回合能开圣冕。

低配启动：

- 第 1 回合：`星火`获得 1 日耀，给目标 1 灼烧。
- 第 1 回合：`日耀：引燃`获得 2 日耀，全敌获得 2 灼烧。
- 第 2 回合：`太阳圣祷`补到 5-6 日耀。
- 第 2 或第 3 回合：`日耀：授冕`进入圣冕窗口。

顺利启动：

- 第 1 回合拿到 4-6 日耀。
- 日耀行动收益开始给超凡。
- 第 2 回合用`日耀：授冕`开窗，随后用`耀焰斩`或跨包终结牌兑现。

数值预估：

- `耀焰斩`在 4 日耀时，基础约 14；圣冕 4+ 时约 18。
- `耀焰斩`在 8 日耀时，基础约 18；圣冕 4+ 时约 26。
- `耀焰斩`在 12 日耀时，基础约 22；圣冕 4+ 时约 34。
- 若期间已经通过日耀获得 40-80 层超凡，实际直接伤害还会约乘以 1.4 到 1.8。

使用边界：

- 星火包不负责杀 50000 以上血量的敌人。
- 星火包的职责是把玩家带进圣冕窗口，并给烬冠/天幕包提供日耀基础。

### 【日耀：烬冠】启动流程

目标手感：先把火吸回来，再用聚炎打出去。爆发前有自燃压力，爆发时有明显兑现。

低配启动：

- 先用`星火`或`日耀：引燃`制造敌方灼烧。
- `引炎`吸收目标最多 6 层灼烧，获得聚炎。
- 自身有灼烧时，`凝烬成塔`把自燃压力转为聚炎。
- 聚炎达到 8-12 时，`燃冠誓言`把聚炎转成全敌灼烧。
- 最后用`炎轮再临`或`炽冕崩落`触发。

标准爆发流程：

- 第 1 阶段：目标身上堆 6-12 灼烧。
- 第 2 阶段：`引炎`和`灼流回收`把灼烧转成 10-20 聚炎。
- 第 3 阶段：`日耀：授冕`开圣冕。
- 第 4 阶段：`燃冠誓言`铺全敌灼烧，`炽冕崩落`结算全体爆发。

数值预估：

- 12 聚炎、4 日耀时，`炽冕崩落`直接伤害约为 40 + 12*5 + 2*10 = 120。
- 24 聚炎、8 日耀时，`炽冕崩落`直接伤害约为 40 + 24*5 + 4*10 = 200。
- 49 聚炎、12 日耀时，`炽冕崩落`直接伤害约为 40 + 49*5 + 6*10 = 345。
- 这些直接伤害不是主要击杀来源，真正的大数来自灼烧高血量百分比结算。

使用边界：

- 对 1000-5000 血敌人，烬冠不需要堆到 49 层，20 层灼烧多次触发已经足够。
- 对 50000-100000 血敌人，烬冠必须依赖 20-49 层灼烧和多次触发，单纯直接伤害不够。
- 若没有圣冕，`炽冕崩落`可以保留自伤反噬，作为强行斩杀的风险按钮。

### 【日耀：天幕】启动流程

目标手感：让一个点燃的敌人变成全场灾厄，再把全场同时引爆。

低配启动：

- `灼热天幕`获得天幕，全体获得灼烧。
- `日蚀`给全敌灼烧和无力，降低敌方压力。
- 选择灼烧最高的敌人，用`燃灾`扩散。
- 用`蚀天之咒`把关键目标灼烧翻倍并触发。

标准群体流程：

- 第 1 阶段：天幕让所有敌人都有灼烧。
- 第 2 阶段：单体通过`蚀天之咒`或其他来源堆到 20-49 灼烧。
- 第 3 阶段：`燃灾`把高层灼烧扩散到其他敌人。
- 第 4 阶段：`冠冕威光`全敌加灼烧，并在天幕存在时全体立即触发。

使用边界：

- 天幕包对多目标战最强。
- 单体 Boss 战中，`蚀天之咒`是关键牌，`燃灾`价值下降。
- 高护盾敌人如果血量不高，天幕包仍然有价值，因为灼烧按当前血量结算，不主要吃普通护盾。

## 灼烧数值预估

以下按官方公式估算：`伤害 = 层数 * (1 + 当前血量/100)`。多次触发会按剩余血量重算，并假设没有免疫、减伤、特殊防护。

### 10 层灼烧

目标当前血量 1000：

- 触发 1 次约 110。
- 触发 2 次合计约 209。
- 触发 3 次合计约 298。

目标当前血量 5000：

- 触发 1 次约 510。
- 触发 2 次合计约 969。
- 触发 3 次合计约 1382。

目标当前血量 10000：

- 触发 1 次约 1010。
- 触发 2 次合计约 1919。
- 触发 3 次合计约 2737。

目标当前血量 50000：

- 触发 1 次约 5010。
- 触发 2 次合计约 9519。
- 触发 3 次合计约 13577。

目标当前血量 100000：

- 触发 1 次约 10010。
- 触发 2 次合计约 19019。
- 触发 3 次合计约 27127。

结论：10 层适合中低血敌人的持续压血。面对 50000 以上血量时，10 层三次触发也只是削掉约 27%，需要继续堆层或翻倍。

### 20 层灼烧

目标当前血量 1000：

- 触发 1 次约 220。
- 触发 2 次合计约 396。
- 触发 3 次合计约 536。

目标当前血量 5000：

- 触发 1 次约 1020。
- 触发 2 次合计约 1836。
- 触发 3 次合计约 2488。

目标当前血量 10000：

- 触发 1 次约 2020。
- 触发 2 次合计约 3636。
- 触发 3 次合计约 4928。

目标当前血量 50000：

- 触发 1 次约 10020。
- 触发 2 次合计约 18036。
- 触发 3 次合计约 24448。

目标当前血量 100000：

- 触发 1 次约 20020。
- 触发 2 次合计约 36036。
- 触发 3 次合计约 48848。

结论：20 层是新版天幕/烬冠的关键阈值。对 100000 血目标，三次触发能打掉接近一半血量；对 10000 血目标，三次触发已接近斩杀线。

### 49 层灼烧

目标当前血量 1000：

- 触发 1 次约 539。
- 触发 2 次合计约 813。
- 触发 3 次合计约 953。

目标当前血量 5000：

- 触发 1 次约 2499。
- 触发 2 次合计约 3773。
- 触发 3 次合计约 4423。

目标当前血量 10000：

- 触发 1 次约 4949。
- 触发 2 次合计约 7472。
- 触发 3 次合计约 8759。

目标当前血量 50000：

- 触发 1 次约 24549。
- 触发 2 次合计约 37068。
- 触发 3 次合计约 43453。

目标当前血量 100000：

- 触发 1 次约 49049。
- 触发 2 次合计约 74063。
- 触发 3 次合计约 86821。

结论：49 层是接近终结的状态。它对高血目标非常强，但多次触发会递减，不能简单理解为每次 49% 血。`蚀天之咒`和`冠冕威光`应该围绕这一层数上限设计。

## 特殊目标分析

### 低血高护盾

假设目标当前血量 1000，但护盾很高。

普通直接伤害大概率先打护盾，斩杀效率低。

20 层灼烧触发 3 次约造成 536 点生命损失。

49 层灼烧触发 3 次约造成 953 点生命损失，接近无视护盾斩杀。

因此低血高护盾目标应由天幕/灼烧路线处理，而不是靠`炽冕崩落`的直接伤害硬砸。

### 高血低护盾

假设目标当前血量 100000，护盾很低。

直接伤害 200-500 的爆发不够看。

20 层灼烧触发 3 次约 48848。

49 层灼烧触发 2 次约 74063，触发 3 次约 86821。

因此高血目标需要先用`蚀天之咒`、`燃灾`、`燃冠誓言`把灼烧推高，再用`冠冕威光`、`炎轮再临`或`炽冕崩落`触发。

### 高血高护盾

这类目标最适合天幕路线。

直接伤害会被护盾拖慢。

灼烧按当前血量计算，层数越高越能跨过护盾压力。

建议设计目标：玩家若能把目标推到 20 层灼烧并触发 2-3 次，应明显看到血条下降；若推到 49 层并触发 2 次，应进入斩杀窗口。

### 低血低护盾

这类目标不需要复杂链路。

星火包和基础攻击就能处理。

设计上不应让低血小怪强迫玩家走完整“天幕 -> 聚炎 -> 圣冕 -> 崩落”流程，否则启动会显得拖。

## 数值设计建议

### 灼烧层数阈值

5 层以下：铺垫状态，主要用于触发条件。

10 层左右：中期可见伤害，适合普通战。

20 层左右：核心爆发阈值，适合精英/Boss。

49 层：终结上限，必须需要多张牌或稀有牌配合。

### 立即触发次数

普通牌最多触发 1 次。

进阶牌在条件满足时触发 1 次，或对单体触发 2 次。

稀有牌允许全体触发 1 次。

`炎轮再临`作为特例，允许 1/2/3 次递增，但必须封顶。

### 聚炎层数阈值

5 聚炎：小攻击或小防御。

8 聚炎：触发抽牌/回费等奖励。

12 聚炎：群体铺灼烧的主要阈值。

20+ 聚炎：进入`炽冕崩落`的爽点区间。

### 日耀层数阈值

4 日耀：进入第一阶段，日耀攻击有明显增强。

8 日耀：圣冕期间可给魔能或更强行动收益。

12 日耀：允许清除自身灼烧或免疫自身天幕灼烧。

日耀不建议继续鼓励无上限堆叠。当前 Buff 上限 15 是合理的。

## 第一轮实施清单

第一步：只改卡包掉落归属和文案，不急着删 ID。

第二步：同步 `Data/Card/sunexp.csv` 与 `Text/Card/sunexp.csv`，把移出掉落的卡从卡包池中剥离或标记为后续重做。

第三步：重写 22 张保留牌的中文描述，优先压短中文，再同步繁中、英文、日文。

第四步：检查所有涉及即时灼烧的脚本，统一使用已验证的 `SetStatus(...); RunImmediately("buff_burn", "StartRound")` 模式。

第五步：调整 `炎轮再临` 的动态费用与触发次数封顶，避免无限增长导致性能和文本问题。

第六步：把 `registry.lua` 的注册清单分组，但保持最终 `Entry.lua` 运行入口不变。

第七步：运行验证：

```powershell
.codex\skills\sunexp-mod-dev\scripts\validate-sunexp.ps1
```

第八步：输出调整后的卡牌效果清单与版本更新文档。

## 风险与待确认

灼烧的 `ChangeHp` 路径是否完全绕过所有护盾和特殊防护，需要进游戏实测。

`RunImmediately("buff_burn", "StartRound")` 在 `Target`、`AllTarget`、`All` 三种状态下的行为要分别测一次。

`炎轮再临`动态费用既涉及静态 `Expend`，也涉及脚本变量 `ExCost`，需要重点回归。

移出掉落的卡不要直接删行。先确认图鉴、旧存档、事件奖励、乌娜技能是否引用这些 ID。

若后续要保留`残光病兆`，必须把“按灼烧一半给易伤”改成固定 1-2 层易伤，否则高层灼烧会造成双重爆炸。
