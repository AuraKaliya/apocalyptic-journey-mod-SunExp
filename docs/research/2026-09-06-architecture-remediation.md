**Terrias / AuraToolsExp 完整整改跟踪**

依据同日 architecture-review 的 A01–A14；本文件在实施与验收后更新，不以计划代替完成。

| 项目 | 实施状态 | 验收 |
|---|---|---|
| A01 完整发布、输入快照、安装依赖 | 已发布并同步完整游戏安装包 | 1,167 文件逐项核验；更新 41 文件；安装中的 32 项 CG 素材依赖通过 |
| A02 联机回放可靠接纳 | 完整输入先交接，再后台入库；incoming journal 恢复 | 队列压力、幂等保存、重启恢复、损坏输入测试通过；联机待实测 |
| A03 调度接纳与替换顺序 | 接纳后才替换；完成后的取消仍交还回执 | 容量拒绝、延迟帧、取消窗口、主线程运行器暂不可用测试通过 |
| A04 后台存储、资料库、锁范围 | 回放库与视频准备异步化；最小骨架、编码复用、锁外解码 | 数据库/回放行为与生产编译通过；大库交互待实测 |
| A05 配置提交与失败回滚 | 失败传播、原对象回滚、独立子模块保留、退役旧镜像写入 | 写入失败、版本冲突、父子对象独立提交测试通过 |
| A06 幂等账本失败处理 | 损坏/不可读不再当作空账本 | 损坏格式、重复键、读写失败及重复 claim 测试通过 |
| A07 纹理预热契约 | Texture2D 统一走原生 Texture；区分加载/缺失/失败 | Unity 纹理类型、缓存别名、销毁重载、空命中测试通过 |
| A08 可靠任务容量预算 | 共用有界 FIFO；持有预算到主线程回执；领域保留未接纳终局 | 数量/字节预算、背压、继续排空测试通过 |
| A09 分层例外切换 | 关闭 15 条，预算 64→49；投影应用入口与 Buff 策略完成切换 | 610 个生产文件语义检查通过；49 条其他能力债务仍登记在册 |
| A10 初始化依赖就绪状态 | Ready/Failed/Blocked；必需路由与绑定失败阻止依赖激活 | 依赖失败与独立模块测试、生产编译通过 |
| A11 局部 UI 更新 | 资料库选中/编辑局部更新，精灵选择复用读模型，AI 页面共享帧状态 | 生产编译通过；交互与负载需游戏内验收 |
| A12 网络角色与接收需求 | 会话、权威与远端成员分别查询；移除旧内部语义别名 | 接收者筛选、RPC 权威扫描、原生类型身份测试通过 |
| A13 分层测试与发布证据 | 独立回放套件、真实延迟帧、Unity 测试、哈希绑定验证收据 | 最终收据含 17 组自动检查；发布、回滚、包完整性和实际安装核验通过 |
| A14 诊断字段与线程/任务阶段 | cacheHit 与 loaded 分开，线程号与排队/后台/主线程耗时分开 | 资源测试与生产编译通过，实战日志待复测 |

约束：保留用户存档、收藏与旧 v17 文档；产品 DLL 使用唯一发布者；需要迁移的旧路径在同一次能力切换中移除；实战与自动化结果分别报告。

修复后的责任边界与失败契约见 [执行与交付契约](2026-09-06-release-and-storage-contracts.md)。分层的剩余债务与关闭条件见 [例外关闭清单](../Terrias/architecture-exception-closures.md)。本轮不把 49 条历史例外登记更新说成已消除，也不把源代码/单元测试通过说成单人或联机实战已通过。

回归中新增确认的两处细节：原生 `RpcCommandBaseSerializer` 使用 `TypeNameHandling.All`，所以网络 DTO 旧完整类型名必须保留；后台任务完成到主线程应用之间也存在取消窗口，可靠队列必须在此窗口接回完成回执。

最终自动化证据：AuraTools 1,529 项断言；Terrias 863 项断言；Shared Core 1,296 项断言；奥莉米娅 48、哥伦比娅 37、归家的月亮 27 项专项断言；Unity 29 项测试；共享 ABI 5,816 项（新增 70 项、移除 0 项）；Terrias 内容/资源、CG 资源闭包、分层、共享写入、RPC 权威与项目技能检查通过。

发布事务 `51357eaf346845508c4f81c9ee8bae9b`，输入指纹 `195D0A5CFE7869E89639191B5D5FBD170A1B1E92964CBD161EF8494455B8B8F5`。最终安装状态 Verified；AuraToolsExp 256 个文件、Terrias 911 个文件，合计核验 1,167 个，变更 41 个。用户存档与 ModsData 未改写。

- [验证收据](../../artifacts/shared-release/Release/validation.json)
- [完整包清单](../../artifacts/shared-release/Release/shared-package-manifest.json)
- [安装与备份收据](../../artifacts/product-deploy/51357eaf346845508c4f81c9ee8bae9b/deployment.json)

两份产品包的 Aura.Shared 哈希均为 `8B04EDE1F5A9C5039F0EEFD2EEBFE8DEFA163848A8ACEF110C86B791B19512AC`；AuraTools Entry 为 `A48351E9E39981D90E1188ACB59932ACF8B09BB252DAA14DA46DB1876780E25B`；Terrias Entry 为 `CC876EF307800D1A731C06401E8B143165D73540918CA39A538491C0472F4F1D`。

部署演练发现日志原子替换的空字符串参数问题，已改为显式保留上一份耐久日志，并在 PowerShell 7 与 Windows PowerShell 5.1 中测试连续替换。此前事务 `428d76d9b992407eb3e38b1ea77194b4` 的 41 个文件已恢复并核验，状态为 RolledBack；最终事务在修正后的流程中重新发布并安装成功。失败记录保留用于审计。

实战验收仍待执行：使用同一角色与战斗规模，开启/关闭对局记录分别战斗并结束；查看资料库、回放和导出；主机/客户端测试断连及接收恢复。观察新的 `queueMs / workerMs / mainApplyMs` 与实际帧时间，不能用后台总耗时或断言数量代替卡顿改善证据。
