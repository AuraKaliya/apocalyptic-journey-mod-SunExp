节点目录由生产定义生成。每个节点的系统名称不可被备注覆盖。

**本牌使用时**（触发入口）

本牌使用时开始一次流程；目标由本次使用提供。

类型 `entry.Use`；节点版本 2。本牌这一时点的唯一入口。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 开始 | 执行 | 子流程或完成出口 |
参数：无需额外配置

示例：本牌使用时 → 给予护盾（使用者，3 点）。


**本牌抽到时**（触发入口）

本牌进入原生抽到时点时执行；没有使用目标。

类型 `entry.Draw`；节点版本 2。本牌这一时点的唯一入口。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 开始 | 执行 | 子流程或完成出口 |
参数：无需额外配置

示例：本牌抽到时 → 给予护盾（使用者，3 点）。


**本牌丢弃时**（触发入口）

本牌原生丢弃时执行；用牌后的弃牌也可能触发。

类型 `entry.Discard`；节点版本 2。本牌这一时点的唯一入口。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 开始 | 执行 | 子流程或完成出口 |
参数：无需额外配置

示例：本牌丢弃时 → 给予护盾（使用者，3 点）。


**造成普通伤害**（卡牌效果）

对指定角色执行造成普通伤害；数量非负并向下取整。

类型 `effect.Damage`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标 | 单个角色 | 默认 使用时选中目标；接线覆盖默认值 |
| 输入 | 伤害值 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；伤害值（填写或接线）

示例：本牌使用时 → 造成普通伤害（选择目标），填写伤害值 3。


**造成真实伤害**（卡牌效果）

对指定角色执行造成真实伤害；数量非负并向下取整。

类型 `effect.TrueDamage`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标 | 单个角色 | 默认 使用时选中目标；接线覆盖默认值 |
| 输入 | 伤害值 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；伤害值（填写或接线）

示例：本牌使用时 → 造成真实伤害（选择目标），填写伤害值 3。


**给予护盾**（卡牌效果）

对指定角色执行给予护盾；数量非负并向下取整。

类型 `effect.Shield`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标 | 单个角色 | 默认 使用者；接线覆盖默认值 |
| 输入 | 数量 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；数量（填写或接线）

示例：本牌使用时 → 给予护盾（选择目标），填写数量 3。


**恢复生命**（卡牌效果）

对指定角色执行恢复生命；数量非负并向下取整。

类型 `effect.Heal`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标 | 单个角色 | 默认 使用者；接线覆盖默认值 |
| 输入 | 数量 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；数量（填写或接线）

示例：本牌使用时 → 恢复生命（选择目标），填写数量 3。


**增加生命上限**（卡牌效果）

对指定角色执行增加生命上限；数量非负并向下取整。

类型 `effect.MaxHealth`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标 | 单个角色 | 默认 使用者；接线覆盖默认值 |
| 输入 | 数量 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；数量（填写或接线）

示例：本牌使用时 → 增加生命上限（选择目标），填写数量 3。


**使用者获得能量**（卡牌效果）

对使用者执行使用者获得能量；数量非负并向下取整。

类型 `effect.Energy`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 数量 | 数值 | 默认 6点；接线覆盖默认值 |
参数：数量（填写或接线）

示例：本牌使用时 → 使用者获得能量（作用于使用者），填写数量 3。


**使用者抽牌**（卡牌效果）

使用者抽取指定张数，入手完成后继续。

类型 `effect.Draw`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 张数 | 数值 | 默认 6张；接线覆盖默认值 |
参数：张数（填写或接线）

示例：本牌使用时 → 使用者抽牌（作用于使用者），填写张数 3。


**施加已有状态**（卡牌效果）

向目标施加游戏已有状态，不创建新的 BUFF。

类型 `effect.AddBuff`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标 | 单个角色 | 默认 使用时选中目标；接线覆盖默认值 |
| 输入 | 层数 | 数值 | 默认 6层；接线覆盖默认值 |
参数：具体状态（必选）；目标或角色（可接线）；层数（填写或接线）

示例：选择一个已加载的状态，设置目标和 3 层 → 接入执行流程。叠加和持续行为由该状态本身决定。


**移除已有状态**（卡牌效果）

移除目标的整个指定状态。

类型 `effect.RemoveBuff`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标 | 单个角色 | 默认 使用时选中目标；接线覆盖默认值 |
参数：具体状态（必选）；目标或角色（可接线）

示例：选择要移除的状态，设置目标 → 接入执行流程。目标身上的该状态会被整个移除，不是减少一层。


**使用者弃牌**（卡牌效果）

使用者选择卡牌；完成后继续，取消会结束本次流程。

类型 `effect.Discard`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 张数 | 数值 | 默认 6张；接线覆盖默认值 |
参数：张数（填写或接线）

示例：本牌使用时 → 使用者弃牌（作用于使用者），填写张数 3。


**焚毁使用者手牌**（卡牌效果）

使用者选择卡牌；完成后继续，取消会结束本次流程。

类型 `effect.Burn`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 张数 | 数值 | 默认 6张；接线覆盖默认值 |
参数：张数（填写或接线）

示例：本牌使用时 → 焚毁使用者手牌（作用于使用者），填写张数 3。


**重洗使用者牌堆**（卡牌效果）

重洗使用者牌堆，沿用原生洗牌规则。

类型 `effect.Shuffle`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
参数：无需额外配置

示例：本牌使用时 → 重洗使用者牌堆（作用于使用者）。 


**从抽牌堆选牌入手**（卡牌效果）

使用者选择卡牌；完成后继续，取消会结束本次流程。

类型 `effect.SelectFromDeck`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 张数 | 数值 | 默认 6张；接线覆盖默认值 |
参数：张数（填写或接线）

示例：本牌使用时 → 从抽牌堆选牌入手（作用于使用者），填写张数 3。


**从弃牌堆选牌入手**（卡牌效果）

使用者选择卡牌；完成后继续，取消会结束本次流程。

类型 `effect.SelectFromDiscard`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 张数 | 数值 | 默认 6张；接线覆盖默认值 |
参数：张数（填写或接线）

示例：本牌使用时 → 从弃牌堆选牌入手（作用于使用者），填写张数 3。


**结束使用者回合**（卡牌效果）

结束使用者回合，并终止本次流程。

类型 `effect.EndTurn`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
参数：无需额外配置

示例：本牌使用时 → 结束使用者回合（作用于使用者）。 


**群体 · 造成普通伤害**（卡牌效果）

对指定集合执行造成普通伤害；数量非负并向下取整。

类型 `effect.Damage.group`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标集合 | 角色集合 | 默认 所有敌人；接线覆盖默认值 |
| 输入 | 伤害值 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；伤害值（填写或接线）

示例：本牌使用时 → 群体 · 造成普通伤害（选择目标），填写伤害值 3。


**群体 · 造成真实伤害**（卡牌效果）

对指定集合执行造成真实伤害；数量非负并向下取整。

类型 `effect.TrueDamage.group`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标集合 | 角色集合 | 默认 所有敌人；接线覆盖默认值 |
| 输入 | 伤害值 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；伤害值（填写或接线）

示例：本牌使用时 → 群体 · 造成真实伤害（选择目标），填写伤害值 3。


**群体 · 给予护盾**（卡牌效果）

对指定集合执行给予护盾；数量非负并向下取整。

类型 `effect.Shield.group`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标集合 | 角色集合 | 默认 所有友方（含使用者）；接线覆盖默认值 |
| 输入 | 数量 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；数量（填写或接线）

示例：本牌使用时 → 群体 · 给予护盾（选择目标），填写数量 3。


**群体 · 恢复生命**（卡牌效果）

对指定集合执行恢复生命；数量非负并向下取整。

类型 `effect.Heal.group`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标集合 | 角色集合 | 默认 所有友方（含使用者）；接线覆盖默认值 |
| 输入 | 数量 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；数量（填写或接线）

示例：本牌使用时 → 群体 · 恢复生命（选择目标），填写数量 3。


**群体 · 增加生命上限**（卡牌效果）

对指定集合执行增加生命上限；数量非负并向下取整。

类型 `effect.MaxHealth.group`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标集合 | 角色集合 | 默认 所有友方（含使用者）；接线覆盖默认值 |
| 输入 | 数量 | 数值 | 默认 6点；接线覆盖默认值 |
参数：目标或角色（可接线）；数量（填写或接线）

示例：本牌使用时 → 群体 · 增加生命上限（选择目标），填写数量 3。


**群体 · 施加已有状态**（卡牌效果）

向目标施加游戏已有状态，不创建新的 BUFF。

类型 `effect.AddBuff.group`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标集合 | 角色集合 | 默认 所有敌人；接线覆盖默认值 |
| 输入 | 层数 | 数值 | 默认 6层；接线覆盖默认值 |
参数：具体状态（必选）；目标或角色（可接线）；层数（填写或接线）

示例：选择一个已加载的状态，设置目标和 3 层 → 接入执行流程。叠加和持续行为由该状态本身决定。


**群体 · 移除已有状态**（卡牌效果）

移除目标的整个指定状态。

类型 `effect.RemoveBuff.group`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 目标集合 | 角色集合 | 默认 所有敌人；接线覆盖默认值 |
参数：具体状态（必选）；目标或角色（可接线）

示例：选择要移除的状态，设置目标 → 接入执行流程。目标身上的该状态会被整个移除，不是减少一层。


**固定数值**（数值与条件）

固定数值或比例；支持小数，百分比 50% 与比例小数 0.5 等价。

类型 `value.Number`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
参数：数值及格式

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**加**（数值与条件）

按所示运算输出数值结果。

类型 `value.Add`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：数值 A（填写或接线）；数值 B（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**减**（数值与条件）

按所示运算输出数值结果。

类型 `value.Subtract`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 被减数 | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 减数 | 数值 | 默认 0；接线覆盖默认值 |
参数：被减数（填写或接线）；减数（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**乘**（数值与条件）

按所示运算输出数值结果。

类型 `value.Multiply`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：数值 A（填写或接线）；数值 B（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**除**（数值与条件）

被除数除以除数；除数为零会停止流程并定位此节点。

类型 `value.Divide`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 被除数 | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 除数 | 数值 | 默认 0；接线覆盖默认值 |
参数：被除数（填写或接线）；除数（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**较小值**（数值与条件）

按所示运算输出数值结果。

类型 `value.Minimum`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：数值 A（填写或接线）；数值 B（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**较大值**（数值与条件）

按所示运算输出数值结果。

类型 `value.Maximum`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：数值 A（填写或接线）；数值 B（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**向下取整**（数值与条件）

按所示运算输出数值结果。

类型 `value.Floor`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 数值 | 数值 | 默认 0；接线覆盖默认值 |
参数：数值（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**向上取整**（数值与条件）

按所示运算输出数值结果。

类型 `value.Ceiling`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 数值 | 数值 | 默认 0；接线覆盖默认值 |
参数：数值（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**限定范围**（数值与条件）

把输入值限制在下限与上限之间。

类型 `value.Clamp`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 值 | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 下限 | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 上限 | 数值 | 默认 0；接线覆盖默认值 |
参数：值（填写或接线）；下限（填写或接线）；上限（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**等于**（数值与条件）

按所示运算输出真假结果。

类型 `value.Equal`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：比较关系；数值 A（填写或接线）；数值 B（填写或接线）

示例：使用者的生命百分比 → 数值 A；数值 B 设为 50% → 条件结果连接‘如果 / 否则’。普通数值与比例需要先明确转换。


**不等于**（数值与条件）

按所示运算输出真假结果。

类型 `value.NotEqual`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：比较关系；数值 A（填写或接线）；数值 B（填写或接线）

示例：使用者的生命百分比 → 数值 A；数值 B 设为 50% → 条件结果连接‘如果 / 否则’。普通数值与比例需要先明确转换。


**大于**（数值与条件）

按所示运算输出真假结果。

类型 `value.Greater`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：比较关系；数值 A（填写或接线）；数值 B（填写或接线）

示例：使用者的生命百分比 → 数值 A；数值 B 设为 50% → 条件结果连接‘如果 / 否则’。普通数值与比例需要先明确转换。


**大于等于**（数值与条件）

按所示运算输出真假结果。

类型 `value.AtLeast`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：比较关系；数值 A（填写或接线）；数值 B（填写或接线）

示例：使用者的生命百分比 → 数值 A；数值 B 设为 50% → 条件结果连接‘如果 / 否则’。普通数值与比例需要先明确转换。


**小于**（数值与条件）

按所示运算输出真假结果。

类型 `value.Less`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：比较关系；数值 A（填写或接线）；数值 B（填写或接线）

示例：使用者的生命百分比 → 数值 A；数值 B 设为 50% → 条件结果连接‘如果 / 否则’。普通数值与比例需要先明确转换。


**小于等于**（数值与条件）

按所示运算输出真假结果。

类型 `value.AtMost`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
参数：比较关系；数值 A（填写或接线）；数值 B（填写或接线）

示例：使用者的生命百分比 → 数值 A；数值 B 设为 50% → 条件结果连接‘如果 / 否则’。普通数值与比例需要先明确转换。


**全部满足**（数值与条件）

两个条件都成立；第一个不成立时不读取第二个。

类型 `value.And`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 条件 1 | 条件 | 必须连接 |
| 输入 | 条件 2 | 条件 | 必须连接 |
参数：条件 1（必须连线）；条件 2（必须连线）

示例：两个数值比较的结果 → 条件接口 → 如果 / 否则；取反只接一个条件。


**任一满足**（数值与条件）

任一条件成立；第一个成立时不读取第二个。

类型 `value.Or`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 条件 1 | 条件 | 必须连接 |
| 输入 | 条件 2 | 条件 | 必须连接 |
参数：条件 1（必须连线）；条件 2（必须连线）

示例：两个数值比较的结果 → 条件接口 → 如果 / 否则；取反只接一个条件。


**条件取反**（数值与条件）

按所示运算输出真假结果。

类型 `value.Not`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 取反结果 | 条件 | 同一入口内按需读取 |
| 输入 | 原条件 | 条件 | 必须连接 |
参数：原条件（必须连线）

示例：两个数值比较的结果 → 条件接口 → 如果 / 否则；取反只接一个条件。


**对象存在**（数值与条件）

判断角色是否仍有效，可用于保护后续属性读取。

类型 `value.Exists`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 角色 | 单个角色 | 默认 使用者；接线覆盖默认值 |
参数：目标或角色（可接线）

示例：角色 → 判断对象有效 → 如果 / 否则；成立分支中再读取属性。


**数值转比例**（数值与条件）

将普通数值明确用作比例：0.5 变为 50%，实际数值保持。

类型 `value.AsRatio`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 输入数值 | 数值 | 默认 0；接线覆盖默认值 |
参数：输入数值（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**比例转小数**（数值与条件）

将比例转为普通小数：50% 变为 0.5，用于明确的数量计算。

类型 `value.AsNumber`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 输入数值 | 数值 | 默认 0；接线覆盖默认值 |
参数：输入数值（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**百分比转数值**（数值与条件）

取百分比的百分数值：50% 变为普通数值 50。

类型 `value.PercentPoints`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 输入数值 | 数值 | 默认 0；接线覆盖默认值 |
参数：输入数值（填写或接线）

示例：填写输入数值或连接取值节点 → 将输出连接到后续数值接口。比例 50% 与比例小数 0.5 等价；整数也是普通数值。


**约等于**（数值与条件）

判断两数之差是否在指定容差内；容差与输入单位一致，且不能为负数。

类型 `value.Approximately`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 判断结果 | 条件 | 同一入口内按需读取 |
| 输入 | 数值 A | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 数值 B | 数值 | 默认 0；接线覆盖默认值 |
| 输入 | 容差 | 数值 | 默认 0；接线覆盖默认值 |
参数：比较关系；数值 A（填写或接线）；数值 B（填写或接线）；容差（填写或接线）

示例：使用者的生命百分比 → 数值 A；数值 B 设为 50% → 条件结果连接‘如果 / 否则’。普通数值与比例需要先明确转换。


**当前生命**（读取数据）

需要该数值时读取指定角色的当前生命。

类型 `value.Read.Health`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 角色 | 单个角色 | 默认 使用者；接线覆盖默认值 |
参数：目标或角色（可接线）；读取属性

示例：当前生命 → 数值比较 → 如果 / 否则。


**生命上限**（读取数据）

需要该数值时读取指定角色的生命上限。

类型 `value.Read.MaxHealth`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 角色 | 单个角色 | 默认 使用者；接线覆盖默认值 |
参数：目标或角色（可接线）；读取属性

示例：生命上限 → 数值比较 → 如果 / 否则。


**已损失生命**（读取数据）

需要该数值时读取指定角色的已损失生命。

类型 `value.Read.MissingHealth`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 角色 | 单个角色 | 默认 使用者；接线覆盖默认值 |
参数：目标或角色（可接线）；读取属性

示例：已损失生命 → 数值比较 → 如果 / 否则。


**生命百分比**（读取数据）

读取指定角色的生命比例，以百分比显示；例如 50% 与比例小数 0.5 等价。

类型 `value.Read.HealthPercent`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 角色 | 单个角色 | 默认 使用者；接线覆盖默认值 |
参数：目标或角色（可接线）；读取属性

示例：生命百分比 → 数值比较 → 如果 / 否则。


**护盾**（读取数据）

需要该数值时读取指定角色的护盾。

类型 `value.Read.Shield`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 角色 | 单个角色 | 默认 使用者；接线覆盖默认值 |
参数：目标或角色（可接线）；读取属性

示例：护盾 → 数值比较 → 如果 / 否则。


**指定状态层数**（读取数据）

读取已有状态层数；未持有为 0，资源缺失是错误。

类型 `value.Read.BuffStacks`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
| 输入 | 角色 | 单个角色 | 默认 使用者；接线覆盖默认值 |
参数：具体状态（必选）；目标或角色（可接线）；读取属性

示例：选择一个已加载的状态，读取使用者持有的层数 → 与 3 比较 → 连接‘如果 / 否则’。未持有该状态时返回 0。


**当前能量**（读取数据）

读取使用者的当前能量。

类型 `value.Read.Energy`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
参数：读取属性

示例：当前能量 → 数值比较 → 如果 / 否则。


**能量上限**（读取数据）

读取使用者的能量上限。

类型 `value.Read.MaxEnergy`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
参数：读取属性

示例：能量上限 → 数值比较 → 如果 / 否则。


**手牌数量**（读取数据）

读取使用者的手牌数量。

类型 `value.Read.HandCount`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
参数：读取属性

示例：手牌数量 → 数值比较 → 如果 / 否则。


**抽牌堆数量**（读取数据）

读取使用者的抽牌堆数量。

类型 `value.Read.DeckCount`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
参数：读取属性

示例：抽牌堆数量 → 数值比较 → 如果 / 否则。


**弃牌堆数量**（读取数据）

读取使用者的弃牌堆数量。

类型 `value.Read.DiscardCount`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
参数：读取属性

示例：弃牌堆数量 → 数值比较 → 如果 / 否则。


**本牌基础费用**（读取数据）

读取本牌基础费用，不代表本次实际支付费用。

类型 `value.Read.CardCost`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 数值 | 数值 | 同一入口内按需读取 |
参数：读取属性

示例：本牌基础费用 → 数值比较 → 如果 / 否则。


**使用者**（对象与选择）

本牌的使用者；固定输出单个角色。

类型 `object.Self`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 角色 | 单个角色 | 同一入口内按需读取 |
参数：无需额外配置

示例：使用者 → 对应类型的目标接口；角色集合可接群体效果或逐个对象执行。


**使用时选中目标**（对象与选择）

本次使用选中的角色；仅限有目标卡牌的使用流程。

类型 `object.Target`；节点版本 2。仅限需要选中目标的卡牌的使用入口。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 角色 | 单个角色 | 同一入口内按需读取 |
参数：无需额外配置

示例：使用时选中目标 → 对应类型的目标接口；角色集合可接群体效果或逐个对象执行。


**所有敌人**（对象与选择）

需要该数据时读取有效角色集合；不持续监控。

类型 `object.Enemies`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 角色集合 | 角色集合 | 同一入口内按需读取 |
参数：无需额外配置

示例：所有敌人 → 对应类型的目标接口；角色集合可接群体效果或逐个对象执行。


**所有友方（含使用者）**（对象与选择）

需要该数据时读取有效角色集合；不持续监控。

类型 `object.Friends`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 角色集合 | 角色集合 | 同一入口内按需读取 |
参数：无需额外配置

示例：所有友方（含使用者） → 对应类型的目标接口；角色集合可接群体效果或逐个对象执行。


**所有角色**（对象与选择）

需要该数据时读取有效角色集合；不持续监控。

类型 `object.All`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输出 | 角色集合 | 角色集合 | 同一入口内按需读取 |
参数：无需额外配置

示例：所有角色 → 对应类型的目标接口；角色集合可接群体效果或逐个对象执行。


**如果 / 否则**（流程控制）

根据条件执行一个分支，然后进入完成出口。

类型 `node.If`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成后 | 执行 | 子流程或完成出口 |
| 输入 | 条件 | 条件 | 必须连接 |
| 输出 | 成立 | 执行 | 子流程或完成出口 |
| 输出 | 不成立 | 执行 | 子流程或完成出口 |
参数：条件（必须连线）

示例：生命百分比小于 50% → 条件；成立 → 给予护盾，不成立 → 抽牌。分支末端返回后执行‘完成后’。


**重复若干次**（流程控制）

按整数次数执行子流程；零次直接完成，上限 64 次。

类型 `node.Repeat`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成后 | 执行 | 子流程或完成出口 |
| 输入 | 次数 | 数值 | 默认 2次；接线覆盖默认值 |
| 输出 | 执行体 | 执行 | 子流程或完成出口 |
参数：次数（填写或接线）

示例：本牌使用时 → 重复 3 次；执行体 → 使用者抽牌 1 张；完成后接下一个效果。


**逐个对象执行**（流程控制）

进入时确定集合，逐个执行；当前对象只在执行体有效。

类型 `node.ForEach`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成后 | 执行 | 子流程或完成出口 |
| 输入 | 对象集合 | 角色集合 | 默认 所有敌人；接线覆盖默认值 |
| 输出 | 执行体 | 执行 | 子流程或完成出口 |
| 输出 | 当前对象 | 单个角色 | 仅限对应遍历体 |
参数：目标或角色（可接线）

示例：所有敌人 → 对象集合；当前对象 → 伤害目标，执行体 → 造成普通伤害。


**记住数值**（本次变量）

保存执行到此处时的数值；后续读取不重新计算。

类型 `node.CaptureNumber`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 捕获数值 | 数值 | 默认 6；接线覆盖默认值 |
| 输出 | 捕获值 | 数值 | 捕获后的同一执行作用域 |
参数：结果名称；捕获数值（填写或接线）

示例：读取生命 → 捕获数值；执行到此处记住生命，再改变生命，捕获值仍保持原值。


**记住对象**（本次变量）

保存对象引用供后续使用；不会冻结其生命等属性。

类型 `node.CaptureObject`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 完成 | 执行 | 子流程或完成出口 |
| 输入 | 捕获对象 | 单个角色 | 必须连接 |
| 输出 | 记住的对象 | 单个角色 | 捕获后的同一执行作用域 |
参数：结果名称；捕获对象（必须连线）

示例：随机选中的对象 → 捕获对象；记住的对象可在之后的同一流程使用。


**结束本次流程**（流程控制）

立即结束本次入口，包括尚未完成的重复。

类型 `node.Stop`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
参数：无需额外配置

示例：如果 / 否则的某一分支 → 结束本次流程，不再执行该入口的剩余效果。


**注释**（画布注释）

画布说明，不参与连线、执行或语义预算。

类型 `node.Note`；节点版本 2。画布元素，不属于执行流程。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
参数：注释内容、尺寸

示例：为一组节点写下注释，说明‘生命较低时补充护盾’；注释不参与执行。

画布元素，没有接口；不计入运行节点数。

**随机选择一个敌人**（对象与选择）

从原生合法敌方集合选择 1 个；每次执行抽样一次。

类型 `node.PickEnemy`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 已选中 | 执行 | 子流程或完成出口 |
| 输出 | 无可选目标 | 执行 | 子流程或完成出口 |
| 输出 | 选中的敌人 | 单个角色 | 只在已选中子流程可用 |
参数：结果名称

示例：已选中 → 造成伤害，结果 → 伤害目标；未选中 → 给予使用者护盾。


**随机选择一个友方**（对象与选择）

从友方集合选择 1 个，包含使用者；每次执行抽样一次。

类型 `node.PickFriend`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输出 | 已选中 | 执行 | 子流程或完成出口 |
| 输出 | 无可选目标 | 执行 | 子流程或完成出口 |
| 输出 | 选中的友方 | 单个角色 | 只在已选中子流程可用 |
参数：结果名称

示例：已选中 → 造成伤害，结果 → 伤害目标；未选中 → 给予使用者护盾。


**从集合随机选择一个角色**（对象与选择）

从输入集合中的有效角色随机选择 1 个。

类型 `node.PickFromSet`；节点版本 2。可用于三类入口；读取使用目标或临时变量时还需满足其作用域。

| 方向 | 接口 | 类型 | 规则 |
| --- | --- | --- | --- |
| 输入 | 执行 | 执行 | 单一执行来源 |
| 输入 | 候选集合 | 角色集合 | 必须连接 |
| 输出 | 已选中 | 执行 | 子流程或完成出口 |
| 输出 | 无可选目标 | 执行 | 子流程或完成出口 |
| 输出 | 选中的角色 | 单个角色 | 只在已选中子流程可用 |
参数：结果名称；候选集合（必须连线）

示例：已选中 → 造成伤害，结果 → 伤害目标；未选中 → 给予使用者护盾。


